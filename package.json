{
  "name": "hackaton-2024-nco",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "lint:stylelint": "npx stylelint '**/*.css' --fix",
    "lint:prettier": "prettier --write .",
    "lint": "yarn lint:stylelint && yarn lint:prettier"
  },
  "devDependencies": {
    "prettier": "3.3.3",
    "vite": "^5.4.1"
  },
  "dependencies": {
    "stylelint": "^16.9.0",
    "stylelint-config-standard": "^36.0.1",
    "stylelint-prettier": "^5.0.2"
  }
}
